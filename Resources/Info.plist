<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- CFBundleName is the label macOS shows in System Settings > Privacy & Security >
	     Calendars. Keep it identical to the binary name so the toggle is recognisable. -->
	<key>CFBundleName</key>
	<string>apple-calendar-mcp</string>
	<key>CFBundleIdentifier</key>
	<string>codes.eneko.apple-calendar-mcp</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>

	<!-- macOS 14 split calendar access into full and write-only. This server needs full
	     access: write-only cannot read back what it created, so it could not confirm an
	     event or enforce the "never edit a past event" rule. Key name verified against
	     the tccd binary on macOS 26.5. -->
	<key>NSCalendarsFullAccessUsageDescription</key>
	<string>Lets Claude read and manage your calendar events through this local MCP server.</string>
</dict>
</plist>
