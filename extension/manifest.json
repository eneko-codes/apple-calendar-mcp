{
  "manifest_version": "0.3",
  "name": "apple-calendar-mcp",
  "display_name": "Apple Calendar",
  "version": "1.1.0",
  "description": "Read and manage macOS Calendar events through EventKit",
  "long_description": "A local MCP server written in Swift that gives Claude access to the Calendar app on this Mac.\n\nThere is no network, no account and no API key. iCloud is only the sync engine that fills the local calendar store. The gate is macOS privacy consent, and the grant appears under System Settings \u2192 Privacy & Security \u2192 Calendars. Full access is required: write-only access cannot read events back, so the server could not confirm what it created.\n\nPlug and play: there is nothing to configure. Every calendar is reachable; the only control over this server is its own per-tool permission switch below.\n\nEventKit cannot invite attendees and cannot create repeating events; both must be done in Calendar.app.\n\nNot affiliated with or endorsed by Apple Inc.",
  "author": {
    "name": "eneko-codes",
    "url": "https://github.com/eneko-codes"
  },
  "homepage": "https://github.com/eneko-codes/apple-calendar-mcp",
  "repository": {
    "type": "git",
    "url": "https://github.com/eneko-codes/apple-calendar-mcp"
  },
  "license": "MIT",
  "icon": "icon.png",
  "keywords": [
    "calendar",
    "events",
    "eventkit",
    "macos",
    "apple",
    "schedule"
  ],
  "server": {
    "type": "binary",
    "entry_point": "server/apple-calendar-mcp",
    "mcp_config": {
      "command": "${__dirname}/server/apple-calendar-mcp",
      "args": [],
      "env": {}
    }
  },
  "tools": [
    {
      "name": "calendar_status",
      "description": "Report whether Calendar access is granted, and how to grant it. Reads no events."
    },
    {
      "name": "calendars_list",
      "description": "List calendars, their accounts, and which accept writes"
    },
    {
      "name": "calendar_search",
      "description": "Find events in a date range, optionally filtered by text and calendar"
    },
    {
      "name": "calendar_get",
      "description": "Read one event in full, including whether it is past, in progress or upcoming"
    },
    {
      "name": "create_event",
      "description": "Add an event. Cannot invite attendees or create a repeating series."
    },
    {
      "name": "update_event",
      "description": "Change an event, including one that has already ended."
    },
    {
      "name": "delete_event",
      "description": "Permanently delete an event, including one that has already ended. Requires explicit confirmation."
    }
  ],
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": [
      "darwin"
    ]
  }
}
